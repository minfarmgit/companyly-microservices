export enum ProtocolToClient {
    MAILS_LIST = 'MAILS_MIST',
}

export enum ProtocolToServer {
    JOIN_CLIENT = 'JOIN_CLIENT',
}

export enum SocketProtocol {
    connection = 'connection',
    disconnect = 'disconnect',
}