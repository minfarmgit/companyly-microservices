{
  "name": "companyly-microservices",
  "version": "1.0.0",
  "description": "",
  "scripts": {
    "postinstall": "npm install --prefix ./assistant && npm install --prefix ./sync && npm install --prefix ./webrtc",
    "dev": "nodemon",
    "assistant:dev": "npm run dev --prefix ./assistant",
    "sync:dev": "npm run dev --prefix ./sync",
    "webrtc:dev": "npm run dev --prefix ./webrtc",
    "build": "npm run build --prefix ./assistant && npm run build --prefix ./sync && npm run build --prefix ./webrtc",
    "start": "npm run start --prefix ./assistant && npm run start --prefix ./sync && npm run start --prefix ./webrtc"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/minfarmgit/companyly-microservices.git"
  },
  "author": "zidiks",
  "license": "ISC",
  "bugs": {
    "url": "https://github.com/minfarmgit/companyly-microservices/issues"
  },
  "homepage": "https://github.com/minfarmgit/companyly-microservices#readme",
  "devDependencies": {
    "npm-run-all": "^4.1.5",
    "@types/express": "^4.17.13",
    "@types/node": "^18.0.3",
    "@types/simple-peer": "^9.11.4",
    "@types/string-similarity": "^4.0.0",
    "nodemon": "^2.0.19",
    "ts-node": "^10.8.2",
    "typescript": "^4.7.4"
  }
}
